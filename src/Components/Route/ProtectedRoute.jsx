// import React from "react";
// import { Navigate, Outlet } from "react-router-dom";
// import { useAuth } from "./AuthContext.jsx";
// import { useRol } from "./RolContext.jsx";

// export const ProtectedRoute = ({ allowedRoles }) => {
// k
//   const { rol } = useRol();
//   console.log(rol);
//   return !rol ? (
//     <Navigate to="/" />
//   ) : rol.find((role) => allowedRoles.includes(role)) ? (
//     <Outlet />
//   ) : (
//     <Navigate to="/" />
//   );
// };
